"""
달력 표시에 쓰는 색.

**공간 색(`zones/palette.py`)과 따로 둔다.** 그쪽은 칩의 *배경*이라 밝은 색까지
쓸 수 있고(글씨는 `onColor` 가 뒤집어 얹는다), 명도 축을 살리려고 일부러 밝은 색을
넣어두었다. 이쪽은 흰 바탕에 놓이는 *글자*다 — 밝은 색을 그대로 쓰면 9px 짜리 절기
이름이 종이에 묻어 사라진다.

그래서 기준이 하나 더 있다: **흰 바탕에서 작은 글씨로 읽힐 것.** 아래 값은 전부
흰색 대비 4.5:1 이상이라 본문 크기 기준을 넘긴다(`tests.py` 가 지킨다).

색만으로 종류를 나누게 두지 않는다 — 달력은 날짜 숫자를 굵게도 하고 이름도 함께
적는다. 색이 안 읽히는 눈에도 무슨 날인지는 남아야 한다.
"""

#  이름을 함께 둔다. 설정에서 고를 때 견본만 늘어놓으면 무엇을 고른 건지 말로
#  확인할 수 없고, 화면 낭독기에는 아예 안 읽힌다.
PALETTE = [
    {"color": "#DC2626", "name": "빨강"},
    {"color": "#EA580C", "name": "주황"},
    {"color": "#16A34A", "name": "초록"},
    {"color": "#0D9488", "name": "청록"},
    {"color": "#2563EB", "name": "파랑"},
    {"color": "#7C3AED", "name": "보라"},
    {"color": "#DB2777", "name": "자홍"},
]

COLORS = [item["color"] for item in PALETTE]
